# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env
*.db
*.sqlite3

# Node / frontend
node_modules/
node_modules
dist/
.vite/

# OS / editor
.DS_Store
*.log

# Infra / secrets (no pushear al repo de auto-deploy)
infra/

# Material de clientes (decks, fichas) — no va al repo
clientes/

# Firebase / FCM service-account secret (admin-app) — NUNCA al repo
admin-app/fcm-service-account.json
admin-app/*firebase-adminsdk*.json
# google-services.json SÍ se trackea (no es secreto; va dentro del APK por diseño)
